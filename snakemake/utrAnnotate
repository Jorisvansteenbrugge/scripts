
rule pita_utr:
	input:
		bed=lambda wildcards: config["bedfiles"][wildcards.sample]
		bam=expand({bam}.bam, bam=config["bamfiles"])
	output:
		"{sample}_utrExtended.bed"
	shell:
		"pita_utr -i {input.bed} -b {input.bam} > {sample}_utrExtended.bed"

